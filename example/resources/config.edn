{:profile #or [#env PROFILE "foo"]
 :api {:server {:port 1002}}

 :oc/system {:web-server #:oc {:init :foobar.server/create
                               :config #ref [:api :server]
                               :dependencies [:widget :now]}

             :now #:oc {:create #oreo/fn :foobar.system/now}

             :widget #:oc {:init :foobar.widget/make
                           :config #ref [:profile]}}}
